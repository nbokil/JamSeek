<!DOCTYPE html>
<html lang="en">

<head>
    <% include ./partials/head %>
</head>

<body>
    <% include ./partials/nav %>
    <div class="container">

        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                    <% if (username == false ) { %>
                        <% include ./partials/login %>
                        <!-- include partials for two buttons, login and register -->
                    <% } else { %>
                        <% include ./partials/home %>
                    <% } %>
                </div>
            </div>
        </div>
    </div><!-- /.container -->

    <footer>
        <% include ./partials/footer %>
    </footer>

</body>

</html>
